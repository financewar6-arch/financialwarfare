// Hand-written editorial content for a War Room page.
// These fields are never generated from the price feed — they're the
// analyst's judgment call, edited directly in each content/<asset>.ts file.
export interface EditorialContent {
  photoLabel: string;
  whyItMoved: string;
  whyYouShouldCare: string;
  risk: string;
  watchNext: string;
}

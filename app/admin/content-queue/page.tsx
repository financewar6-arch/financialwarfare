import { ContentQueueDashboard } from "@/components/admin/ContentQueueDashboard";

export const metadata = { title: "Content Queue — Financial Warfare Admin" };

export default function ContentQueuePage() {
  return <ContentQueueDashboard />;
}
